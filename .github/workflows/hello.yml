name: Professional Workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository 
        uses: actions/checkout@v4

      - name: Show files
        run: ls

      - name: Print Current working directory
        run: pwd

      - name: Print Success message
        run: echo "Wrokflow running successfully"