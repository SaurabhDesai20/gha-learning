name: Dependency Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Building App"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - run: echo "Deploying App"